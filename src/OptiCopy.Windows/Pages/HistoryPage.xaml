<Page
    x:Class="OptiCopy.Windows.Pages.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28,22,28,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <StackPanel Spacing="4">
                <TextBlock Text="TRANSFER HISTORY" FontSize="24" FontWeight="SemiBold" Foreground="{ThemeResource OptiTextPrimaryBrush}" />
                <TextBlock Text="Local record of optical transfer sessions" Foreground="{ThemeResource OptiTextMutedBrush}" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                <Button Content="Refresh" Click="Refresh_Click" />
                <Button Content="Clear" Click="Clear_Click" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Margin="0,20,0,12" Padding="14"
                Background="{ThemeResource OptiSurfaceBrush}"
                BorderBrush="{ThemeResource OptiCardBorderBrush}" BorderThickness="1" CornerRadius="14">
            <TextBlock x:Name="SummaryLabel" Text="Loading history…" Foreground="{ThemeResource OptiTextSecondaryBrush}" />
        </Border>

        <ListView x:Name="HistoryList" Grid.Row="2" IsItemClickEnabled="False">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Padding="14" Margin="0,0,0,8"
                            Background="{ThemeResource OptiSurfaceBrush}"
                            BorderBrush="{ThemeResource OptiCardBorderBrush}" BorderThickness="1" CornerRadius="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="130" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="140" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="{Binding DirectionLabel}" FontWeight="SemiBold" Foreground="{ThemeResource OptiTextPrimaryBrush}" />
                                <TextBlock Text="{Binding StatusLabel}" Margin="0,4,0,0" Foreground="{ThemeResource OptiTextMutedBrush}" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="18,0,12,0">
                                <TextBlock Text="{Binding FileName}" FontWeight="SemiBold" Foreground="{ThemeResource OptiTextPrimaryBrush}" />
                                <TextBlock Text="{Binding Details}" Margin="0,4,0,0" Foreground="{ThemeResource OptiTextMutedBrush}" TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding TimestampLabel}" Foreground="{ThemeResource OptiTextSecondaryBrush}" TextAlignment="Right" />
                                <TextBlock Text="{Binding HashLabel}" Margin="0,4,0,0" FontSize="11" Foreground="{ThemeResource OptiTextMutedBrush}" TextAlignment="Right" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
